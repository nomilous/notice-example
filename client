console.log('seconds grace (waiting for hub)')
setTimeout( function(){
    require('./lib/client').start( function(error) {
        if( error ) {
            process.stdout.write(error.stack + '\n');
            process.exit(error.errno || 1);
        }
    })
}, 1000);
