module.exports.MessageBus = require('notice').hub()

    #
    # notice.hub() is a `Definition` factory
    # --------------------------------------
    # 
    # * It creates the Definition ("faux class") to be used for creating a Hub
    # * Definition.create() makes an instance of the Hub
    #

